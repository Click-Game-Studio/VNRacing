# CU-THEME — Theme Change

## Phạm vi
Theme Change (#400) — grouping Feature: Customize Room (CU-ROOM #299) và Main Menu (CU-MENU #320). Feature này là content+UI, không phải subsystem mới. Xem audit của CU-ROOM và CU-MENU.

## Module/class C++ liên quan (file thật)
Feature này không có subsystem riêng. Code hiện có nằm trong:
- `UCarCustomizationManager` (CU-ROOM)
- `UCustomizeCarSubsystem` (CU-ROOM)
- Main Menu level + UI shell (CU-MENU)

## Audit Blueprint
Xem audit của CU-ROOM và CU-MENU.

## Mức ưu tiên: **P2**
Feature grouping mới — không có code mới để audit.
