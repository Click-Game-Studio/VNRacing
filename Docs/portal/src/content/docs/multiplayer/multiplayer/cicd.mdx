---
title: CI/CD LinuxServer Build
description: Trạng thái CI/CD hiện tại và kế hoạch bổ sung stage LinuxServer / Edgegap Publish.
---

## Trạng thái CI/CD hiện tại

Pipeline hiện tại (RacingMobile.groovy) chỉ build iOS + Android:

- Perforce checkout
- Build editor (iOS + Android)
- Android: internal artifact → Nexus; Shipping AAB → Google Play
- iOS: package → xcarchive → TestFlight upload
- Discord notifications

**Chưa có stage nào** cho LinuxServer + Edgegap publish + Docker image push.

## Stage mới: LinuxServer / Edgegap Publish

Stage mới chạy **song song** với iOS và Android. Dùng `Build/Edgegap/publish_edgegap_server.sh`.

### Tham số

| Tham số | Kiểu | Mặc định | Mô tả |
|---|---|---|---|
| LINUXSERVER_MODE | choice | skip | LinuxServer build mode: skip, package, publish |
| EDGEGAP_VERSION | text | (auto) | Edgegap version tag. Auto: `<app>-<yyyymmdd>-r<P4_CL>` |
| EDGEGAP_DRY_RUN | boolean | true | Nếu bật, chỉ dry-run, không push thật |

### Biến môi trường cần thêm

| Biến | Nguồn | Mô tả |
|---|---|---|
| LINUX_MULTIARCH_ROOT | env | Đường dẫn UE5 Linux cross-compile toolchain |
| EDGEGAP_API_TOKEN | credentials | Edgegap API token |
| EDGEGAP_CONTAINER_REGISTRY_PROJECT | credentials | Container registry project |
| EDGEGAP_PRIVATE_REGISTRY_USERNAME | credentials | Registry username |
| EDGEGAP_PRIVATE_REGISTRY_TOKEN | credentials | Registry token |
| UE5_LINUX_EXE | env | Tùy chọn: Linux UnrealEditor-Cmd override |

### Checklist thiết lập

- [ ] Thêm Jenkins credential: `edgegap-api-token-prototype-racing`, `edgegap-registry-project`, `edgegap-registry-username`, `edgegap-registry-token`
- [ ] Tạo agent label `linux_builder` (Linux host có UE5 toolchain, Docker, mạng ra Edgegap)
- [ ] Mount broker-flow.env từ secret store
- [ ] Test `--dry-run` trước
- [ ] Test `--skip-package` khi đã có LinuxServer archive sẵn
- [ ] Test full publish lên staging app
- [ ] Verify LinuxServer boot vào Multiplayer_WaitingRoom
- [ ] Verify Nakama runtime đọc được EDGEGAP_APP_VERSION mới
